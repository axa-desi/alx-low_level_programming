#include "main.h"

/**
 * main - entry point
 * Return: Always 0
 */

void print_alphabet_x10(void)
{
	char b;
	int x = 0;

	while (x++ <= 9)
	{
		for (b = 'a'; b <= 'z'; b++);
	{
	_putchar(b);
	}
	_putchar('\n');
	}
}
