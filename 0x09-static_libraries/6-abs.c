#include "main.h"
/**
 *_abs - cmputes the absolute value of an integer
 *@n: the integer to check
 *Return: the absolute value of then integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return(n);
	}
	return(-n);
}
