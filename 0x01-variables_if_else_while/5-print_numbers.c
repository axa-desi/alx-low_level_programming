#include <stdio.h>

/**
  * main - Entry point
  *
  * Description: print number of base 10 form 0
  * Return: Always 0
  */
int main(void)
{
	int x;

	for (x = 0; x <= 10; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
